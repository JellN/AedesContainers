import 'package:app/app/modules/perfil/perfil_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';


void main() {
  initModule(PerfilModule());
  // PassController pass;
  //
  setUp(() {
    //     pass = PerfilModule.to.get<PassController>();
  });

  group('PassController Test', () {
    //   test("First Test", () {
    //     expect(pass, isInstanceOf<PassController>());
    //   });

    //   test("Set Value", () {
    //     expect(pass.value, equals(0));
    //     pass.increment();
    //     expect(pass.value, equals(1));
    //   });
  });
}
