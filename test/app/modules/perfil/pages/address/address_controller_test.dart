import 'package:app/app/modules/perfil/perfil_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';



void main() {
  initModule(PerfilModule());
  // AddressController address;
  //
  setUp(() {
    //     address = PerfilModule.to.get<AddressController>();
  });

  group('AddressController Test', () {
    //   test("First Test", () {
    //     expect(address, isInstanceOf<AddressController>());
    //   });

    //   test("Set Value", () {
    //     expect(address.value, equals(0));
    //     address.increment();
    //     expect(address.value, equals(1));
    //   });
  });
}
