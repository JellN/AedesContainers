import 'package:app/app/modules/perfil/perfil_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';



void main() {
  initModule(PerfilModule());
  // EmailController email;
  //
  setUp(() {
    //     email = PerfilModule.to.get<EmailController>();
  });

  group('EmailController Test', () {
    //   test("First Test", () {
    //     expect(email, isInstanceOf<EmailController>());
    //   });

    //   test("Set Value", () {
    //     expect(email.value, equals(0));
    //     email.increment();
    //     expect(email.value, equals(1));
    //   });
  });
}
