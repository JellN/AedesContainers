import 'package:app/app/modules/perfil/perfil_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';



void main() {
  initModule(PerfilModule());
  // FaceController face;
  //
  setUp(() {
    //     face = PerfilModule.to.get<FaceController>();
  });

  group('FaceController Test', () {
    //   test("First Test", () {
    //     expect(face, isInstanceOf<FaceController>());
    //   });

    //   test("Set Value", () {
    //     expect(face.value, equals(0));
    //     face.increment();
    //     expect(face.value, equals(1));
    //   });
  });
}
