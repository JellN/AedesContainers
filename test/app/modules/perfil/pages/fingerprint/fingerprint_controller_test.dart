import 'package:app/app/modules/perfil/perfil_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';



void main() {
  initModule(PerfilModule());
  // FingerprintController fingerprint;
  //
  setUp(() {
    //     fingerprint = PerfilModule.to.get<FingerprintController>();
  });

  group('FingerprintController Test', () {
    //   test("First Test", () {
    //     expect(fingerprint, isInstanceOf<FingerprintController>());
    //   });

    //   test("Set Value", () {
    //     expect(fingerprint.value, equals(0));
    //     fingerprint.increment();
    //     expect(fingerprint.value, equals(1));
    //   });
  });
}
