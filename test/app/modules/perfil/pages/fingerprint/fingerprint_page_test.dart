import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';


void main() {
  testWidgets('FingerprintPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FingerprintPage(title: 'Fingerprint')));
    //  final titleFinder = find.text('Fingerprint');
    //  expect(titleFinder, findsOneWidget);
  });
}
